#### Description

The `google services` command group lists available Google Workspace services that can be used with `aux4 google auth login --services`.

Available subcommands:

- **list** — List all available services

#### Usage

```bash
aux4 google services <subcommand>
```

#### Example

```bash
aux4 google services list
```
