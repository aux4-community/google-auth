#### Description

The `logout` command clears all saved credentials and the token cache. After logging out, you will need to run `aux4 google auth login` again to re-authenticate.

#### Usage

```bash
aux4 google auth logout
```

#### Example

```bash
aux4 google auth logout
```

```text
Credentials cleared.
```
